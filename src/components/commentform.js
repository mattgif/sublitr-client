import React from 'react';

export default function CommentForm(props) {
    return(
        <form>
            <h4>Comment:</h4>
            <textarea></textarea>
            <button type="submit">Submit</button>
        </form>
    )
}